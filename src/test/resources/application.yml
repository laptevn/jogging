spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:test

jogging:
  scheduling:
    enable: false

weather:
  provider:
    url: http://api.apixu.com/v1
    key: my_secret_key